<div class="ui top fixed menu">
  <div class="item">
    <%= link_to image_tag("chats.png", style: "width: 30px; height: 30px;"), root_path %>
  </div>
  <div class="ui pointing dropdown link item">
    <span class="text">Chatroom</span>
    <i class="dropdown icon"></i>
    <div class="menu">
      <div class="item">Option 1</div>
    </div>
  </div>
    <% if logged_in? %>
      <%= link_to "Log Out", logout_path, data: { turbo_method: :delete, turbo_confirm: "Are you sure you want to log out?" }, class: "item" %>
    <% else %>
      <%= link_to "Log In", login_path, class: "item" %>
    <% end %>
</div>
